import { barColor } from '../../../helpers/bar-color';
import { module, test } from 'qunit';

module('Unit | Helper | bar color');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = barColor(42);
  assert.ok(result);
});
