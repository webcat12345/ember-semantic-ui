import { barWidth } from '../../../helpers/bar-width';
import { module, test } from 'qunit';

module('Unit | Helper | bar width');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = barWidth(42);
  assert.ok(result);
});
