import { absNum } from '../../../helpers/abs-num';
import { module, test } from 'qunit';

module('Unit | Helper | abs num');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = absNum(42);
  assert.ok(result);
});
