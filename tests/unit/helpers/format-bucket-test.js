import { formatBucket } from '../../../helpers/format-bucket';
import { module, test } from 'qunit';

module('Unit | Helper | format bucket');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatBucket(42);
  assert.ok(result);
});
